/**
 * useSchedule
 *
 * Hook placeholder para acceder a los horarios de colectivos.
 * La lógica se implementará cuando los datos estén disponibles.
 *
 * Responsabilidades futuras:
 * - Obtener los horarios del día actual o de un día específico.
 * - Exponer estados de carga y error.
 * - Cachear los datos para evitar re-fetching innecesario.
 *
 * @example
 * const { schedules, isLoading, error } = useSchedule('lunes');
 */
export function useSchedule() {
  // TODO: implementar lógica
  throw new Error('useSchedule: aún no implementado');
}
