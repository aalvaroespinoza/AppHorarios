/**
 * useRecommendations
 *
 * Hook placeholder para obtener las recomendaciones de colectivo
 * según el día y las materias a cursar.
 *
 * Responsabilidades futuras:
 * - Cruzar horarios de materias con horarios de colectivos.
 * - Calcular el margen de tiempo disponible.
 * - Retornar recomendaciones ordenadas por prioridad.
 *
 * @example
 * const { recommendations, isLoading } = useRecommendations('lunes');
 */
export function useRecommendations() {
  // TODO: implementar lógica
  throw new Error('useRecommendations: aún no implementado');
}
